#!/bin/zsh

# Nyan Cat animated progress bar widget for ranma.
#
# Prerequisites:
#   - python3 (macOS built-in)
#   - reap (brew install typester/reap/reap)

export PATH="$PATH:/opt/homebrew/bin"

SCRIPT_DIR="${0:A:h}"

reap --watch $PPID "$SCRIPT_DIR/plugins/nyan_cat.py" &
